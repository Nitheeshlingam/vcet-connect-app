import 'package:flutter/material.dart';

class MailTiming extends StatelessWidget {
  const MailTiming({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
