import 'package:flutter/material.dart';

class AppTheme {
  static const Gradient pageBackground = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Color(0xFFE6F0FF), // Light blue
      Colors.white,
    ],
  );
} 